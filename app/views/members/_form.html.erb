<%= form_with(model: member) do |form| %>
  <% @errors = member&.errors || @user&.errors&.errors || [] %>
  <% if @errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@errors.count, "error") %> prohibited this member from being saved:</h2>

      <ul>
        <% @errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.select :role, options_for_select(Member.roles.map {|k,v| k}) %>
  <%= form.hidden_field :family_id, value: params[:family] || @member.family.id %>
  <%= form.hidden_field :user_id, value: 1 %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
